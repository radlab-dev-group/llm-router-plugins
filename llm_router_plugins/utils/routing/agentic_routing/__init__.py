"""
Agentic routing sub-packages.

Holds :mod:`~llm_router_plugins.utils.routing.agentic_routing.codex`, the
``CodexRoutingPlugin`` (plugin name ``agentic_routing_codex``) for Codex CLI
requests. This module is intentionally empty: it exists so that the directory
stays a regular package and ``agentic_routing.codex`` keeps being packaged.
"""
